# Python 3.12 / Windows / VS Code + Jupyter


# --- Numerical / statistics core (GRS + HAC engine, time-series regressions) ---
numpy
pandas
scipy
statsmodels            # GRS F-test, HAC (Newey-West) standard errors

# --- Visualization (heatmaps, sub-period decomposition) ---
matplotlib

# --- Korean market data collection ---
pykrx                  # prices, market cap, ticker lists, KRX PBR (B/M source). Requires KRX login
OpenDartReader         # book-equity (BE) loader (controlling-interest equity). Not used by the primary B/M path
finance-datareader     # delisting history (KRX-DELISTING), current listings (KRX-DESC), delisted-stock prices
PublicDataReader       # ECOS risk-free CD91 (statistics table 817Y002 / item 010502000)

# --- US baseline ---
pandas-datareader      # Ken French Data Library loader

# --- Environment / execution ---
python-dotenv          # loads KRX/OpenDART/ECOS keys from .env
ipykernel              # Jupyter kernel

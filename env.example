# KRX Data Marketplace 회원 계정 (https://data.krx.co.kr 에서 무료 가입)
# 이 파일을 .env 로 복사한 뒤 실제 값으로 채운다.
# .env 는 절대 git에 커밋하지 않는다 (.gitignore에 추가).
KRX_ID=your_krx_id
KRX_PW=your_krx_password

DART_API_KEY

ECOS_API_KEY